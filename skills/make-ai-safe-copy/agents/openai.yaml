interface:
  display_name: "Make AI-Safe Copy"
  short_description: "Create a local safe copy before an AI reads files."
  default_prompt: "Use $make-ai-safe-copy to sanitize these files locally, then read only the safe copy."
